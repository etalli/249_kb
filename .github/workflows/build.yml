name: Build ZMK firmware

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build.yml@main
    with:
      board: seeeduino_xiao_ble
      shield: xiao60
